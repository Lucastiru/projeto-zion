'use client';
import { useEffect, useMemo, useState } from 'react';
import { AlertTriangle, BarChart3, CalendarDays, Camera, Check, CheckCircle2, ChevronLeft, ChevronRight, CircleAlert, FileText, GripVertical, LayoutList, Maximize2, MoreHorizontal, PanelLeftClose, Pause, Pencil, Play, Plus, Radio, Settings, Trash2, Users, X } from 'lucide-react';

type Moment={id:number;title:string;duration:number;owner:string;details:string;done?:boolean};
type Issue={id:number;time:string;type:string;description:string;status:'Aberto'|'Resolvido'};
type PrepItem={id:number;team:string;text:string;done:boolean;assigned?:string};
type ChurchEvent={id:number;date:string;time:string;title:string;type:string;location:string};
type Volunteer={id:number;name:string;team:string;phone:string;email:string;scheduled:boolean;photo?:string};
const initialMoments:Moment[]=[
 {id:1,title:'Abertura da sala',duration:10,owner:'Equipe de recepção',details:'Fundo padrão e Spotify abertura',done:true},
 {id:2,title:'Countdown',duration:5,owner:'Mídia',details:'Vídeo countdown padrão',done:true},
 {id:3,title:'Abertura',duration:5,owner:'Giseli',details:'Boas-vindas e oração',done:true},
 {id:4,title:'Adoração',duration:40,owner:'João e Hellen',details:'Fundos + letras • Voz e violão'},
 {id:5,title:'Oferta',duration:5,owner:'Fernanda Correia',details:'Fundo padrão + dados bancários'},
 {id:6,title:'Fluxo da oferta',duration:3,owner:'João e Hellen',details:'Porque Ele vive (D)'},
 {id:7,title:'Avisos e transição',duration:5,owner:'Lucas Correia',details:'Engajamento bíblico e avisos'},
 {id:8,title:'Palavra',duration:40,owner:'Josh Veras',details:'Fundo padrão + palavra'},
 {id:9,title:'Encerramento',duration:5,owner:'Josh Veras',details:'Fila de ministração'},
];
const prepSeed=[['Palco','Mesa e água posicionadas',false],['Palco','Púlpito conferido',true],['Mídia','Apresentação e letras testadas',true],['Mídia','Vídeo countdown testado',true],['Som','Microfones e retornos testados',true],['Som','Instrumentos e pedais conferidos',true],['Pessoas','Pregador confirmou presença',true],['Pessoas','Todos os responsáveis avisados',true],['Recepção','Portas e equipe posicionadas',true]] as const;
const nav=[['calendar','Calendário',CalendarDays],['live','Operação ao vivo',Radio],['schedule','Cronograma',LayoutList],['prep','Preparação',CheckCircle2],['volunteers','Voluntários',Users],['issues','Problemas',CircleAlert],['report','Relatório',BarChart3]] as const;
function safeDuration(value:number){return Number.isFinite(value)&&value>0?value:1}
function asTime(start:string,plus:number){const parts=start.split(':').map(Number);const h=Number.isFinite(parts[0])?parts[0]:0;const m=Number.isFinite(parts[1])?parts[1]:0;const total=h*60+m+(Number.isFinite(plus)?plus:0);return `${String(Math.floor(total/60)%24).padStart(2,'0')}:${String(total%60).padStart(2,'0')}`}

export default function Home(){
 const[view,setView]=useState('calendar');const[moments,setMoments]=useState(initialMoments);const[current,setCurrent]=useState(3);const[seconds,setSeconds]=useState(1122);const[running,setRunning]=useState(false);const[editing,setEditing]=useState<Moment|null>(null);const[prep,setPrep]=useState<PrepItem[]>(prepSeed.map((x,i)=>({id:i,team:x[0],text:x[1],done:x[2]})));const[prepEditing,setPrepEditing]=useState<PrepItem|null>(null);const[issues,setIssues]=useState<Issue[]>([{id:1,time:'19:52',type:'Mídia',description:'Arquivo do countdown precisou ser reiniciado.',status:'Resolvido'}]);const[issueOpen,setIssueOpen]=useState(false);const[eventOpen,setEventOpen]=useState(false);const[events,setEvents]=useState<ChurchEvent[]>([{id:1,date:'2026-09-06',time:'19:45',title:'Culto Ekletos',type:'Culto',location:'Auditório principal'},{id:2,date:'2026-09-06',time:'09:30',title:'Culto da Família',type:'Culto',location:'Auditório principal'},{id:3,date:'2026-09-12',time:'20:00',title:'Conferência Zion',type:'Conferência',location:'Auditório principal'},{id:4,date:'2026-09-20',time:'18:00',title:'Encontro de Líderes',type:'Reunião',location:'Sala 2'}]);const[selectedEvent,setSelectedEvent]=useState(1);const[start]=[events.find(e=>e.id===selectedEvent)?.time??'19:45'];
 const[moreOpen,setMoreOpen]=useState(false);const[profileOpen,setProfileOpen]=useState(false);const[volunteerEditing,setVolunteerEditing]=useState<Volunteer|null>(null);const[sidebarCollapsed,setSidebarCollapsed]=useState(false);const[liveFullscreen,setLiveFullscreen]=useState(false);const[volunteers,setVolunteers]=useState<Volunteer[]>([{id:1,name:'Lucas Correia',team:'Manager',phone:'(11) 99999-1001',email:'lucas@zion.church',scheduled:true},{id:2,name:'João Silva',team:'Louvor',phone:'(11) 99999-1002',email:'joao@zion.church',scheduled:true},{id:3,name:'Hellen Souza',team:'Louvor',phone:'(11) 99999-1003',email:'hellen@zion.church',scheduled:true},{id:4,name:'Fernanda Correia',team:'Palco',phone:'(11) 99999-1004',email:'fernanda@zion.church',scheduled:true},{id:5,name:'Rafael Lima',team:'Mídia',phone:'(11) 99999-1005',email:'rafael@zion.church',scheduled:false},{id:6,name:'Marcos Reis',team:'Som',phone:'(11) 99999-1006',email:'marcos@zion.church',scheduled:false}]);
 useEffect(()=>{if(!running)return;const t=setInterval(()=>setSeconds(s=>Math.max(0,s-1)),1000);return()=>clearInterval(t)},[running]);
 const timings=useMemo(()=>{let sum=0;return moments.map(m=>{const duration=safeDuration(m.duration);const time=asTime(start,sum);sum+=duration;return{...m,duration,time,end:asTime(start,sum)}})},[moments,start]);const total=moments.reduce((a,m)=>a+safeDuration(m.duration),0);const prepared=prep.filter(x=>x.done).length;const active=timings[current]??timings[0];const next=timings[current+1];
 function saveMoment(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const duration=safeDuration(Number(f.get('duration')));const isNew=!editing||editing.id===0;const data={id:isNew?Date.now():editing.id,title:String(f.get('title')).trim(),duration,owner:String(f.get('owner')).trim(),details:String(f.get('details')).trim()};setMoments(ms=>isNew?[...ms,data]:ms.map(m=>m.id===data.id?{...m,...data}:m));setEditing(null)}
 function savePrep(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const isNew=!prepEditing||prepEditing.id===0;const data={id:isNew?Date.now():prepEditing.id,team:String(f.get('team')).trim(),text:String(f.get('text')).trim(),assigned:String(f.get('assigned')||''),done:prepEditing?.done??false};setPrep(v=>isNew?[...v,data]:v.map(x=>x.id===data.id?data:x));setPrepEditing(null)}
 function saveEvent(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const item={id:Date.now(),date:String(f.get('date')),time:String(f.get('time')),title:String(f.get('title')).trim(),type:String(f.get('type')),location:String(f.get('location')).trim()};setEvents(v=>[...v,item]);setSelectedEvent(item.id);setEventOpen(false)}
 function saveVolunteer(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const file=f.get('photo') as File;const isNew=!volunteerEditing||volunteerEditing.id===0;const item:Volunteer={id:isNew?Date.now():volunteerEditing.id,name:String(f.get('name')).trim(),team:String(f.get('team')),phone:String(f.get('phone')).trim(),email:String(f.get('email')).trim(),scheduled:volunteerEditing?.scheduled??false,photo:file&&file.size?URL.createObjectURL(file):volunteerEditing?.photo};setVolunteers(v=>isNew?[...v,item]:v.map(x=>x.id===item.id?item:x));setVolunteerEditing(null)}
 function complete(){setMoments(ms=>ms.map((m,i)=>i===current?{...m,done:true}:m));setRunning(false);if(current<moments.length-1){setCurrent(current+1);setSeconds(safeDuration(moments[current+1].duration)*60)}else{setSeconds(0);setView('report')}}
 function addIssue(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);setIssues(v=>[{id:Date.now(),time:new Date().toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'}),type:String(f.get('type')),description:String(f.get('description')),status:'Aberto'},...v]);setIssueOpen(false)}
 return <main className={`app-shell ${sidebarCollapsed?'sidebar-collapsed':''} ${liveFullscreen?'live-fullscreen':''}`}><aside className="sidebar"><div className="brand"><img src="/zion-logo.png" alt="Zion Church"/><span><b>ZION</b><small>CHURCH • ORDEM</small></span><button aria-label="Recolher menu" onClick={()=>setSidebarCollapsed(!sidebarCollapsed)}><PanelLeftClose size={16}/></button></div><nav>{nav.map(([id,label,Icon])=><button key={id} className={`nav-item ${view===id?'active':''}`} onClick={()=>setView(id)}><Icon size={17}/>{label}{id==='issues'&&issues.some(i=>i.status==='Aberto')?<b className="nav-dot"/>:null}</button>)}</nav><div className="side-caption"><span>EVENTO SELECIONADO</span><strong>{events.find(e=>e.id===selectedEvent)?.title}</strong><small>{events.find(e=>e.id===selectedEvent)?.time} • {events.find(e=>e.id===selectedEvent)?.date.split('-').reverse().join('/')}</small></div><button className="sidebar-footer" onClick={()=>setProfileOpen(true)}><div className="avatar">LC</div><div><strong>Lucas Correia</strong><small>Manager</small></div><Settings size={15}/></button></aside>
 <section className="workspace"><header className="topbar"><div><p className="eyebrow">ZION CHURCH • {events.find(e=>e.id===selectedEvent)?.type.toUpperCase()}</p><h1>{events.find(e=>e.id===selectedEvent)?.title} • {events.find(e=>e.id===selectedEvent)?.date.split('-').reverse().join('/')}</h1></div><div className="top-actions"><span className="status-pill"><span/> Equipe conectada</span>{view==='live'&&<button className="ghost-btn" onClick={()=>setLiveFullscreen(!liveFullscreen)}><Maximize2 size={14}/> {liveFullscreen?'Sair da tela cheia':'Tela cheia'}</button>}<button className="ghost-btn" onClick={()=>setView('schedule')}>Editar cronograma</button><div className="more-wrap"><button className="icon-btn" aria-label="Mais opções" onClick={()=>setMoreOpen(!moreOpen)}><MoreHorizontal size={19}/></button>{moreOpen&&<div className="more-menu"><button onClick={()=>{setView('calendar');setMoreOpen(false)}}>Trocar evento</button><button onClick={()=>{setView('volunteers');setMoreOpen(false)}}>Ver escala do dia</button><button onClick={()=>{setView('report');setMoreOpen(false)}}>Abrir relatório</button></div>}</div></div></header>
 <div className="page-wrap">{view==='live'&&<><div className="content-grid"><section className="main-column"><div className="live-card"><div className="live-top"><span className="live-label"><Radio size={14}/> AGORA • {active.time}</span><span className="ahead">No horário</span></div><div className="live-body"><div><p>{active.title}</p><h2>{active.owner}</h2><span>{active.details}</span></div><div className="timer"><strong>{String(Math.floor(seconds/60)).padStart(2,'0')}:{String(seconds%60).padStart(2,'0')}</strong><span>de {active.duration}:00</span></div></div><div className="progress"><i style={{width:`${100-seconds/(active.duration*60)*100}%`}}/></div><div className="live-actions"><button className="primary-btn" onClick={()=>setRunning(!running)}>{running?<Pause size={16} fill="currentColor"/>:<Play size={16} fill="currentColor"/>}{running?'Pausar':'Iniciar cronômetro'}</button><button className="secondary-btn" onClick={complete}>Concluir momento</button><button className="issue-button" onClick={()=>setIssueOpen(true)}><AlertTriangle size={15}/> Registrar problema</button></div></div><div className="section-heading"><div><p className="eyebrow">ROTEIRO</p><h3>Cronograma do culto</h3></div><span className="summary-time">Término previsto {asTime(start,total)}</span></div><div className="schedule-list">{timings.map((item,i)=><div className={`schedule-row ${item.done?'done':''} ${i===current?'live':''}`} key={item.id}><div className="time"><strong>{item.time}</strong><span>{item.duration} min</span></div><div className="node">{item.done?<Check size={13}/>:i===current?<span/>:null}</div><div className="moment"><strong>{item.title}</strong><span>{item.owner}</span></div><button className="row-btn" onClick={()=>setCurrent(i)}><ChevronRight size={17}/></button></div>)}</div></section><aside className="right-panel"><div className="next-card"><p className="eyebrow">A SEGUIR • {next?.time}</p><h3>{next?.title}</h3><p>{next?.owner}</p><div className="mini-check">{prep.slice(0,3).map(x=><span key={x.id} className={!x.done?'pending':''}>{x.done?<Check size={14}/>:<i/>}{x.text}</span>)}</div><button onClick={()=>setView('prep')}>Ver preparação <ChevronRight size={15}/></button></div><div className="attention-card"><div className="attention-icon"><CheckCircle2 size={18}/></div><div><strong>{prepared===prep.length?'Tudo preparado':`${prepared} de ${prep.length} itens prontos`}</strong><p>{prepared===prep.length?'A equipe confirmou todos os itens.':'Revise os itens pendentes antes do próximo momento.'}</p></div></div></aside></div></>}
 {view==='calendar'&&<CalendarView events={events} selectedEvent={selectedEvent} choose={(id)=>{setSelectedEvent(id);setView('live')}} open={()=>setEventOpen(true)}/>} {view==='schedule'&&<ScheduleView timings={timings} total={total} start={start} setEditing={setEditing} setMoments={setMoments}/>} {view==='prep'&&<PrepView prep={prep} setPrep={setPrep} edit={setPrepEditing} volunteers={volunteers}/>} {view==='volunteers'&&<VolunteersView volunteers={volunteers} setVolunteers={setVolunteers} edit={setVolunteerEditing}/>} {view==='issues'&&<IssuesView issues={issues} setIssues={setIssues} open={()=>setIssueOpen(true)}/>} {view==='report'&&<ReportView moments={moments} timings={timings} issues={issues} prepared={prepared} prepTotal={prep.length}/>}</div></section>
 {editing!==null&&<MomentModal moment={editing.id===0?null:editing} close={()=>setEditing(null)} save={saveMoment}/>} {prepEditing!==null&&<PrepModal item={prepEditing.id===0?null:prepEditing} volunteers={volunteers} close={()=>setPrepEditing(null)} save={savePrep}/>} {issueOpen&&<IssueModal close={()=>setIssueOpen(false)} save={addIssue}/>} {eventOpen&&<EventModal close={()=>setEventOpen(false)} save={saveEvent}/>} {volunteerEditing!==null&&<VolunteerModal item={volunteerEditing.id===0?null:volunteerEditing} close={()=>setVolunteerEditing(null)} save={saveVolunteer}/>} {profileOpen&&<ProfileModal close={()=>setProfileOpen(false)}/>}</main>
}

function CalendarView({events,selectedEvent,choose,open}:{events:ChurchEvent[];selectedEvent:number;choose:(id:number)=>void;open:()=>void}){const days=[null,...Array.from({length:30},(_,i)=>i+1)];const week=['SEG','TER','QUA','QUI','SEX','SÁB','DOM'];return <div className="surface calendar-surface"><div className="view-head"><div><p className="eyebrow">AGENDA DA IGREJA</p><h2>Calendário de eventos</h2><p>Escolha um evento para abrir sua operação, preparação e cronograma.</p></div><button className="primary-solid" onClick={open}><Plus size={16}/> Novo evento</button></div><div className="calendar-toolbar"><button><ChevronLeft size={17}/></button><strong>Setembro 2026</strong><button><ChevronRight size={17}/></button><span>{events.length} eventos</span></div><div className="calendar-grid">{week.map(w=><div className="weekday" key={w}>{w}</div>)}{days.map((day,i)=>day===null?<div className="calendar-day muted-day" key={`b${i}`}/>:<div className={`calendar-day ${day===6?'today':''}`} key={day}><span className="day-number">{day}</span><div className="day-events">{events.filter(e=>Number(e.date.slice(-2))===day).map(e=><button className={`calendar-event ${e.id===selectedEvent?'selected':''}`} key={e.id} onClick={()=>choose(e.id)}><strong>{e.time}</strong><span>{e.title}</span></button>)}</div></div>)}</div><div className="calendar-hint"><CalendarDays size={17}/><span><strong>Mais de um evento no mesmo dia?</strong> Cada cartão é independente. Clique no evento desejado para abrir o cronograma correspondente.</span></div></div>}
function ScheduleView({timings,total,start,setEditing,setMoments}:{timings:(Moment&{time:string;end:string})[];total:number;start:string;setEditing:(m:Moment)=>void;setMoments:React.Dispatch<React.SetStateAction<Moment[]>>}){return <div className="surface"><div className="view-head"><div><p className="eyebrow">PLANEJAMENTO</p><h2>Editor de cronograma</h2><p>Organize cada momento. Os horários são recalculados automaticamente.</p></div><div className="view-actions"><button className="ghost-btn"><FileText size={15}/> Duplicar último culto</button><button className="primary-solid" onClick={()=>setEditing({id:0,title:'',duration:5,owner:'',details:''})}><Plus size={16}/> Novo momento</button></div></div><div className="timeline-summary"><div><span>Início</span><strong>{start}</strong></div><ChevronRight/><div><span>Duração</span><strong>{Math.floor(total/60)}h {total%60}min</strong></div><ChevronRight/><div><span>Término previsto</span><strong>{asTime(start,total)}</strong></div><span className="valid"><Check size={14}/> Cronograma válido</span></div><div className="editor-list">{timings.map((m)=><div className="editor-row" key={m.id}><GripVertical size={18}/><div className="editor-time"><strong>{m.time}</strong><span>até {m.end}</span></div><div className="editor-main"><strong>{m.title}</strong><span>{m.owner} • {m.details}</span></div><b>{m.duration} min</b><button onClick={()=>setEditing(m)}><Pencil size={15}/></button><button className="danger-icon" onClick={()=>setMoments(v=>v.filter(x=>x.id!==m.id))}><Trash2 size={15}/></button></div>)}</div><button className="add-row" onClick={()=>setEditing({id:0,title:'',duration:5,owner:'',details:''})}><Plus size={16}/> Adicionar momento</button></div>}
function PrepView({prep,setPrep,edit,volunteers}:{prep:PrepItem[];setPrep:React.Dispatch<React.SetStateAction<PrepItem[]>>;edit:(x:PrepItem)=>void;volunteers:Volunteer[]}){const groups=[...new Set(prep.map(x=>x.team))];const ready=prep.filter(x=>x.done).length;return <div className="surface"><div className="view-head"><div><p className="eyebrow">ANTES DO CULTO</p><h2>Central de preparação</h2><p>Cada tarefa pode ter um voluntário escalado como responsável.</p></div><div className="view-actions"><div className="readiness"><strong>{ready}/{prep.length}</strong><span>itens prontos</span></div><button className="primary-solid" onClick={()=>edit({id:0,team:'Palco',text:'',done:false})}><Plus size={16}/> Novo item</button></div></div><div className="readiness-bar"><i style={{width:`${prep.length?ready/prep.length*100:0}%`}}/></div><div className="prep-grid">{groups.map(g=><section className="team-card" key={g}><div className="team-title"><div className="team-icon"><Users size={16}/></div><div><strong>{g}</strong><span>{volunteers.filter(v=>v.team===g&&v.scheduled).map(v=>v.name).join(', ')||'Sem voluntário escalado'}</span></div></div>{prep.filter(x=>x.team===g).map(x=><div className={`check-row ${x.done?'checked':''}`} key={x.id}><label><input type="checkbox" checked={x.done} onChange={()=>setPrep(v=>v.map(i=>i.id===x.id?{...i,done:!i.done}:i))}/><span className="fake-check">{x.done&&<Check size={13}/>}</span><span>{x.text}{x.assigned&&<small className="assigned-name">{x.assigned}</small>}</span></label><button onClick={()=>edit(x)}><Pencil size={13}/></button><button onClick={()=>setPrep(v=>v.filter(i=>i.id!==x.id))}><Trash2 size={13}/></button></div>)}</section>)}</div></div>}
function VolunteersView({volunteers,setVolunteers,edit}:{volunteers:Volunteer[];setVolunteers:React.Dispatch<React.SetStateAction<Volunteer[]>>;edit:(v:Volunteer)=>void}){const teams=[...new Set(volunteers.map(v=>v.team))];return <div className="surface"><div className="view-head"><div><p className="eyebrow">USUÁRIOS E ESCALA</p><h2>Voluntários</h2><p>Cada voluntário tem perfil próprio e pode ser selecionado para o evento.</p></div><button className="primary-solid" onClick={()=>edit({id:0,name:'',team:'Palco',phone:'',email:'',scheduled:false})}><Plus size={16}/> Cadastrar voluntário</button></div><div className="roster-summary"><div><strong>{volunteers.filter(v=>v.scheduled).length}</strong><span>na escala de hoje</span></div><div className="roster-chips">{teams.map(t=><span key={t}>{t} • {volunteers.filter(v=>v.team===t&&v.scheduled).length}</span>)}</div></div><div className="volunteer-list">{volunteers.map(v=><div className="volunteer-row" key={v.id}><div className="volunteer-avatar">{v.photo?<img src={v.photo} alt=""/>:v.name.split(' ').map(x=>x[0]).slice(0,2).join('')}</div><div><strong>{v.name}</strong><span>{v.team} • {v.email}</span></div><button className="edit-volunteer" onClick={()=>edit(v)}><Pencil size={14}/> Editar</button><label className="schedule-toggle"><input type="checkbox" checked={v.scheduled} onChange={()=>setVolunteers(list=>list.map(x=>x.id===v.id?{...x,scheduled:!x.scheduled}:x))}/><i/>{v.scheduled?'Escalado':'Adicionar à escala'}</label></div>)}</div></div>}
function IssuesView({issues,setIssues,open}:{issues:Issue[];setIssues:React.Dispatch<React.SetStateAction<Issue[]>>;open:()=>void}){return <div className="surface"><div className="view-head"><div><p className="eyebrow">ACOMPANHAMENTO</p><h2>Problemas e ocorrências</h2><p>Registre o que aconteceu e acompanhe a resolução.</p></div><button className="primary-solid" onClick={open}><Plus size={16}/> Registrar problema</button></div><div className="metrics"><div><strong>{issues.length}</strong><span>Total registrado</span></div><div><strong>{issues.filter(x=>x.status==='Aberto').length}</strong><span>Em aberto</span></div><div><strong>{issues.filter(x=>x.status==='Resolvido').length}</strong><span>Resolvidos</span></div></div><div className="issue-list">{issues.length===0?<div className="empty"><CheckCircle2/><strong>Nenhuma ocorrência</strong><span>Quando algo acontecer, registre aqui.</span></div>:issues.map(i=><div className="issue-row" key={i.id}><div className="issue-badge"><AlertTriangle size={16}/></div><div><span>{i.time} • {i.type}</span><strong>{i.description}</strong></div><button className={i.status==='Resolvido'?'resolved':'open-status'} onClick={()=>setIssues(v=>v.map(x=>x.id===i.id?{...x,status:x.status==='Aberto'?'Resolvido':'Aberto'}:x))}>{i.status}</button></div>)}</div></div>}
function ReportView({moments,timings,issues,prepared,prepTotal}:{moments:Moment[];timings:(Moment&{time:string;end:string})[];issues:Issue[];prepared:number;prepTotal:number}){return <div className="surface"><div className="view-head"><div><p className="eyebrow">PÓS-CULTO</p><h2>Relatório da operação</h2><p>Resumo automático para a liderança.</p></div><button className="primary-solid"><FileText size={16}/> Exportar relatório</button></div><div className="report-hero"><div><span>Pontualidade</span><strong>No horário</strong><small>Início 19:45 • Término previsto {timings.at(-1)?.end}</small></div><div className="score">92<small>/100</small></div></div><div className="metrics four"><div><strong>{moments.filter(x=>x.done).length}/{moments.length}</strong><span>Momentos concluídos</span></div><div><strong>{prepared}/{prepTotal}</strong><span>Preparação</span></div><div><strong>{issues.length}</strong><span>Ocorrências</span></div><div><strong>{issues.filter(x=>x.status==='Aberto').length}</strong><span>Pendências</span></div></div><section className="feedback-box"><p className="eyebrow">FEEDBACK DO MANAGER</p><textarea placeholder="Conte para a liderança o que funcionou bem, o que precisa melhorar e quais decisões devem ser tomadas no próximo culto..."/><button className="primary-solid">Salvar feedback</button></section></div>}
function MomentModal({moment,close,save}:{moment:Moment|null;close:()=>void;save:(e:React.FormEvent<HTMLFormElement>)=>void}){return <div className="modal-backdrop"><form className="modal" onSubmit={save}><div className="modal-head"><div><p className="eyebrow">CRONOGRAMA</p><h3>{moment?'Editar momento':'Novo momento'}</h3></div><button type="button" onClick={close}><X/></button></div><label>Nome do momento<input name="title" required defaultValue={moment?.title}/></label><div className="form-grid"><label>Duração (min)<input name="duration" type="number" min="1" required defaultValue={moment?.duration??5}/></label><label>Responsável<input name="owner" required defaultValue={moment?.owner}/></label></div><label>Orientações para a equipe<textarea name="details" defaultValue={moment?.details}/></label><div className="modal-actions"><button type="button" className="ghost-btn" onClick={close}>Cancelar</button><button className="primary-solid">Salvar momento</button></div></form></div>}
function PrepModal({item,volunteers,close,save}:{item:PrepItem|null;volunteers:Volunteer[];close:()=>void;save:(e:React.FormEvent<HTMLFormElement>)=>void}){return <div className="modal-backdrop"><form className="modal" onSubmit={save}><div className="modal-head"><div><p className="eyebrow">PREPARAÇÃO</p><h3>{item?'Editar item':'Novo item'}</h3></div><button type="button" onClick={close}><X/></button></div><label>Equipe<select name="team" defaultValue={item?.team??'Palco'}><option>Palco</option><option>Mídia</option><option>Som</option><option>Pessoas</option><option>Recepção</option><option>Iluminação</option><option>Louvor</option></select></label><label>O que precisa ser preparado?<input name="text" required defaultValue={item?.text} placeholder="Ex.: Testar microfone do pregador"/></label><label>Voluntário responsável<select name="assigned" defaultValue={item?.assigned??''}><option value="">Ainda não definido</option>{volunteers.filter(v=>v.scheduled).map(v=><option key={v.id}>{v.name}</option>)}</select></label><div className="modal-actions"><button type="button" className="ghost-btn" onClick={close}>Cancelar</button><button className="primary-solid">Salvar item</button></div></form></div>}
function EventModal({close,save}:{close:()=>void;save:(e:React.FormEvent<HTMLFormElement>)=>void}){return <div className="modal-backdrop"><form className="modal" onSubmit={save}><div className="modal-head"><div><p className="eyebrow">CALENDÁRIO</p><h3>Novo evento</h3></div><button type="button" onClick={close}><X/></button></div><label>Nome do evento<input name="title" required placeholder="Ex.: Culto Ekletos"/></label><div className="form-grid event-fields"><label>Data<input name="date" type="date" required defaultValue="2026-09-06"/></label><label>Horário<input name="time" type="time" required defaultValue="19:45"/></label></div><div className="form-grid"><label>Tipo<select name="type"><option>Culto</option><option>Conferência</option><option>Reunião</option><option>Ensaio</option><option>Outro</option></select></label><label>Local<input name="location" defaultValue="Auditório principal"/></label></div><div className="modal-actions"><button type="button" className="ghost-btn" onClick={close}>Cancelar</button><button className="primary-solid">Criar evento</button></div></form></div>}
function VolunteerModal({item,close,save}:{item:Volunteer|null;close:()=>void;save:(e:React.FormEvent<HTMLFormElement>)=>void}){return <div className="modal-backdrop"><form className="modal" onSubmit={save}><div className="modal-head"><div><p className="eyebrow">USUÁRIO</p><h3>{item?'Editar voluntário':'Cadastrar voluntário'}</h3></div><button type="button" onClick={close}><X/></button></div><label className="photo-upload"><Camera size={18}/><span>{item?.photo?'Trocar foto de perfil':'Adicionar foto de perfil'}</span><input name="photo" type="file" accept="image/*"/></label><label>Nome completo<input name="name" required defaultValue={item?.name} placeholder="Nome do voluntário"/></label><label>E-mail de acesso<input name="email" type="email" required defaultValue={item?.email} placeholder="nome@zion.church"/></label><div className="form-grid"><label>Time<select name="team" defaultValue={item?.team??'Palco'}><option>Palco</option><option>Mídia</option><option>Som</option><option>Louvor</option><option>Recepção</option><option>Iluminação</option><option>Manager</option></select></label><label>Telefone<input name="phone" defaultValue={item?.phone} placeholder="(11) 99999-9999"/></label></div><p className="user-note">O usuário receberá acesso individual conforme sua função.</p><div className="modal-actions"><button type="button" className="ghost-btn" onClick={close}>Cancelar</button><button className="primary-solid">Salvar usuário</button></div></form></div>}
function ProfileModal({close}:{close:()=>void}){return <div className="modal-backdrop"><div className="modal"><div className="modal-head"><div><p className="eyebrow">MEU PERFIL</p><h3>Lucas Correia</h3></div><button onClick={close}><X/></button></div><div className="profile-card"><div className="avatar large">LC</div><div><strong>Lucas Correia</strong><span>Manager • Zion Church</span></div></div><label>Nome<input defaultValue="Lucas Correia"/></label><label>Função<select defaultValue="Manager"><option>Manager</option><option>Líder</option><option>Voluntário</option></select></label><div className="modal-actions"><button className="ghost-btn" onClick={close}>Cancelar</button><button className="primary-solid" onClick={close}>Salvar alterações</button></div></div></div>}
function IssueModal({close,save}:{close:()=>void;save:(e:React.FormEvent<HTMLFormElement>)=>void}){return <div className="modal-backdrop"><form className="modal" onSubmit={save}><div className="modal-head"><div><p className="eyebrow">OCORRÊNCIA</p><h3>Registrar problema</h3></div><button type="button" onClick={close}><X/></button></div><label>Área<select name="type"><option>Palco</option><option>Mídia</option><option>Som</option><option>Pessoas</option><option>Horário</option><option>Outro</option></select></label><label>O que aconteceu?<textarea name="description" required placeholder="Descreva de forma objetiva..."/></label><div className="modal-actions"><button type="button" className="ghost-btn" onClick={close}>Cancelar</button><button className="primary-solid">Registrar ocorrência</button></div></form></div>}
